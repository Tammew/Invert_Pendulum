; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32-s3-pico_can]
; --- Hardware Configuration ---
platform = espressif32
; 💡 แนะนำให้ใช้ board ID มาตรฐานที่มีโอกาสสำเร็จสูงในการ upload
;    แม้ว่าบอร์ดจริงจะเป็น Pico แต่ DevKitC-1 มี config flash ที่ใกล้เคียง
board = esp32-s3-devkitc-1 
framework = arduino

; --- Performance and Serial Settings ---
board_build.f_cpu = 240000000L   ; ตั้งค่าความเร็ว CPU สูงสุด (240 MHz)
monitor_speed = 115200          ; ความเร็วสำหรับ Serial Monitor
upload_speed = 921600           ; ความเร็ว Upload (ถ้า fails ให้ลอง 460800)
upload_port = /dev/ttyACM0     ; กำหนดพอร์ต upload (ปรับตามระบบของคุณ)
monitor_port = /dev/ttyACM0    ; กำหนดพอร์ต Serial Monitor (ปรับตามระบบของคุณ)

; --- TWAI/CAN Specific Configuration ---
; TWAI/CAN driver ถูก include อัตโนมัติเมื่อใช้ ESP32 framework
; เราจะเน้น build flags ที่ช่วยด้าน performance และ core usage

build_flags = 
    ; 💡 รันโค้ด Arduino และ Core 1 (Control Logic) เพื่อลดภาระ Core 0 (OS/WiFi)
    -DARDUINO_RUNNING_CORE=1
    -DARDUINO_USB_CDC_ON_BOOT=0 ; ปิด Serial USB ชั่วคราว (ถ้าคุณใช้ Native USB)